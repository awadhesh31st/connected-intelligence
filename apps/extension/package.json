{
  "name": "@chatbot/ask-this-page",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension: ask questions about the current webpage and see exactly where the answer comes from.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@chatbot/tsconfig": "workspace:*",
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
